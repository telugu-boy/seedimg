if(FILTERS_ENABLED)
    add_subdirectory(seedimg-filters-core)
endif()
if(CCONV_ENABLED)
    add_subdirectory(seedimg-filters-cconv)
endif()
if(OPENCL_ENABLED)
  add_subdirectory(seedimg-filters-ocl)
endif()
